{
  "buildCommand": "[ -d wastewise ] && (cd wastewise && npm install && npm run build) || (npm install && npm run build && mkdir -p wastewise/dist && cp -r dist/* wastewise/dist/ 2>/dev/null || true)",
  "outputDirectory": "wastewise/dist"
}